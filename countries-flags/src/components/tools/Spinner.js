import React from 'react';

const Spinner = () => {
  return (
    <div className="mt-5" style={{
      display: "flex",
      justifyContent: "center"
    }}>
      <h1>Loading...</h1>
    </div>
  );
};

export default Spinner;
